# v2.0 Scope — Browser/WASM Frontend + Performance

Status: **Proposed** (scoping). Target version: **2.0.0**.
Builds on the v1.0 MVP; grounds its technical choices in
[docs/research/04-language-evaluation.md](research/04-language-evaluation.md).

## 1. Theme

v2.0 delivers on the two original headline goals the MVP deferred:

1. **"Portable core, two frontends"** — compile `tte-core` to WebAssembly and add a
   **browser frontend** that renders the same character grid to an HTML canvas. The
   engine becomes shareable by URL, not just runnable in a terminal.
2. **"Performant"** — a **performance push**: tile-based multithreaded rasterization
   and a SIMD inner loop, with speedups tracked in CI.

> **Pairing assumption:** the v2.0 milestone bundles **WASM frontend (Phase 5)** +
> **Performance (Phase 6)**. They reinforce each other — WASM SIMD/threads are exactly
> the perf primitives report 04 evaluated. If the intended second theme was instead
> *rendering fidelity* or *DSL/UX*, swap Phase 6 and this scope still stands.

## 2. Why these two, now

- The whole architecture was built **library-first** precisely so a second frontend
  could slot in without touching the engine (project brief D1/D4: rendering produces a
  pure `Framebuffer`/`CellBuffer`; presentation is a swappable layer). Phase 5 cashes
  that in and *proves the thesis*.
- Performance was the stated headline from day one. The MVP correctly deferred it
  (research 05: at terminal cell counts, I/O bound, not raster bound) — but the **browser
  canvas and high-resolution half-block modes raise effective pixel counts** to where
  SIMD + threads matter. Phase 6 lands when there's finally a reason for it.

## 3. Key decisions

| # | Decision | Rationale |
|---|---|---|
| V1 | **Keep the "text out" identity in the browser**: the canvas draws the cell grid as monospace glyphs with per-cell color (ASCILINE-style), not a switch to pixel rendering. Half-block mode already gives pixel-like fidelity while staying "text". | Stays true to the project's identity; reuses the existing presenter seam. |
| V2 | **New `tte-wasm` cdylib crate** with a thin `wasm-bindgen` API over `tte-core`; all logic stays in the (already-tested) core. | Mirrors the `tte-cli` thin-frontend pattern; keeps wasm glue minimal. |
| V3 | **Browser frame export lives in core** as a pure function (`Framebuffer` → compact per-cell `{glyph, fg, bg}` buffer), so it's unit-tested natively and the wasm layer only marshals bytes. | Same testability seam as the terminal presenters (docs/02 §4). |
| V4 | **Toolchain: `wasm-pack` (target web) on stable Rust**; static `web/` demo (index.html + JS glue), no framework. Binary-size budget tracked. | Report 04: most mature wasm story; stable, tens-of-KB output. |
| V5 | **Phase 5 ships single-threaded, stable wasm** (wasm SIMD128 optional, no threads). **Threads are a Phase 6 stretch** via a separate nightly build (wasm-bindgen-rayon + COOP/COEP headers). | Report 04: wasm threads need nightly + std rebuild + cross-origin isolation — real but opt-in cost; don't block the frontend on it. |
| V6 | **Performance is feature-gated and behavior-preserving**: `parallel` (rayon tiles) and `simd` (via `wide`/`std::arch` on stable, not nightly `std::simd`) features; **output must stay byte-identical to the scalar path** (the existing golden frames are the regression guard). | Report 04 SIMD-in-Rust-2025; determinism (NFR-1) already enforced by golden frames. |
| V7 | **Native build enables `parallel` by default; wasm uses scalar** (stable wasm has no threads) unless the threaded nightly variant is built. | Keeps both targets green on stable. |

## 4. Phase 5 — Browser / WASM frontend (the headline)

**Deliverable:** open a web page, see a model/scene rendered as a colored character grid
on a canvas, orbit it with the mouse, and edit a scene live in a textarea.

| ID | Requirement |
|---|---|
| FR-5.1 | `tte-wasm` cdylib exposing a `wasm-bindgen` `Renderer`: construct with cell `width`/`height`; `load_obj(text)` and `load_scene(text)` (DSL); `set_orbit(yaw, pitch, radius)`; `render()`. Parse/DSL errors surface as JS exceptions with the line-numbered message. |
| FR-5.2 | Core browser-frame export: a pure `tte-core` function `Framebuffer` → a compact per-cell representation (glyph + packed `fg`/`bg` RGB), unit-tested natively. The wasm layer returns it as typed arrays (no per-cell JS calls). |
| FR-5.3 | Browser frontend (`web/`): a canvas that draws the cell grid (monospace glyphs, per-cell color); **mouse-drag orbit + wheel zoom** mapped to `OrbitCamera`; a **live scene editor** (textarea → re-render on change, browser-side hot-reload); a few built-in presets (cube, sphere, sample scene). |
| FR-5.4 | Build pipeline: `wasm-pack build --target web` produces the wasm module + JS bindings; a documented build script and a static, servable `web/` directory. Works offline (no CDN deps). |
| FR-5.5 | `wasm-bindgen-test` smoke test: construct a `Renderer`, load the cube, `render()`, assert a non-empty, expected-dimension frame; runs under headless browser/node and is wired into a CI job. |

### Phase 5 non-functional

| ID | Requirement | Verification |
|---|---|---|
| NFR-7 | WASM release binary under a size budget (target **≤250 KB**, ideally <150 KB gz) with `opt-level="z"` + `wasm-opt`. | CI size check (warn over budget) |
| NFR-8 | Default scene renders interactively in the browser (**≥30 FPS** at ~120×60 cells on a modern laptop). | manual + a perf note in the demo |
| NFR-9 | Identical scene/model produces the **same frame natively and in WASM** (modulo float determinism). | shared golden frame compared across targets |

## 5. Phase 6 — Performance push

**Deliverable:** the same frames, produced measurably faster on multi-core CPUs and via
SIMD, with the speedups visible in `cargo bench` / CI.

| ID | Requirement |
|---|---|
| FR-6.1 | **Tile-based multithreaded rasterization** (native): partition the framebuffer into tiles, rasterize triangles into tiles in parallel with `rayon`; race-free by tile ownership. Behind a `parallel` feature (on by default natively, off for stable wasm). |
| FR-6.2 | **SIMD inner loop**: vectorize the edge-function/barycentric coverage + attribute interpolation using `wide`/`std::arch` (stable), behind a `simd` feature with a scalar fallback and runtime/`cfg` selection. |
| FR-6.3 | **Behavior preservation**: parallel and SIMD paths produce **byte-identical** output to the scalar path — asserted by running the existing golden-frame suite against every feature combination in CI. |
| FR-6.4 | **Benchmark expansion**: criterion benches over large meshes (≥100k triangles) and high cell resolutions (e.g. 400×200 half-block); scalar vs `parallel` vs `simd` comparisons; baselines tracked as CI artifacts. |
| FR-6.5 *(stretch)* | **WASM threads**: a nightly build variant using `wasm-bindgen-rayon` + a demo served with COOP/COEP headers, parallelizing the browser rasterizer. Clearly separated from the stable default build. |

### Phase 6 non-functional

| ID | Requirement | Verification |
|---|---|---|
| NFR-10 | `parallel` gives a **≥3× speedup** over scalar on a large mesh at high resolution on an 8-core CPU. | criterion comparison |
| NFR-11 | `simd` gives a **≥2× speedup** on the rasterizer fill stage (single-thread). | criterion micro-bench |
| NFR-12 | All feature combinations (`--no-default-features`, `parallel`, `simd`, both) pass the full test + golden-frame suite. | CI feature matrix |

## 6. Verification strategy

- **Golden frames are the safety net for the whole release**: every perf path and the
  WASM export must reproduce the v1.0 golden frames byte-for-byte. This is the strongest
  possible regression guard and is already in place (docs/02 §3).
- **WASM**: `wasm-bindgen-test` for the glue; the heavy logic is covered by existing
  native `tte-core` tests. A shared golden frame compares native vs wasm output.
- **CI grows**: a `wasm` job (`wasm-pack test` + size check) and a `features` matrix job
  (scalar/parallel/simd) join the existing fmt/clippy/test/deny pipeline.

## 7. Roadmap & estimates

| Phase | Scope | Est. |
|---|---|---|
| **5. Browser/WASM frontend** | `tte-wasm` crate, core frame export, canvas + mouse + live editor, wasm-pack build, smoke test, CI wasm job | 20–35 h |
| **6. Performance push** | rayon tiles, SIMD fill, feature matrix + golden parity, expanded benches; (stretch) wasm threads | 25–45 h |

v2.0 total: **~45–80 h**. Phase 5 is independently shippable (could be a v1.1 if you'd
rather split); Phase 6 lands the v2.0 headline.

## 8. Risks

1. **WASM threads' cross-origin isolation** (COOP/COEP) complicates hosting and is
   nightly-only (report 04). *Mitigation:* keep it a clearly-separated stretch (FR-6.5);
   the default browser build is stable + scalar and ships regardless.
2. **SIMD/parallel determinism drift** — reordering or fused-multiply-add could perturb
   a pixel and break golden frames. *Mitigation:* FR-6.3 makes byte-identical output a
   hard requirement; treat any diff as a bug, not a snapshot update.
3. **Canvas text-rendering throughput** in the browser (drawing thousands of colored
   glyphs per frame). *Mitigation:* batch by color run (like the terminal SGR merging),
   consider a glyph atlas; spike it early (below).
4. **Binary-size creep** from wasm-bindgen/web-sys. *Mitigation:* NFR-7 budget + wasm-opt,
   minimal web-sys feature selection.

## 9. Phase-start research spikes (de-risking, ~½–1 day each)

Run these *before* committing the detailed Phase 5/6 design, in lieu of a full upfront
research pass:

- **S1 (Phase 5):** fastest way to draw a per-cell colored character grid to a canvas at
  ≥30 FPS — `fillText` batched by color vs. a pre-rendered glyph atlas via `drawImage`/
  `putImageData`. Decides FR-5.3's renderer.
- **S2 (Phase 5):** confirm current `wasm-pack`/`wasm-bindgen` ergonomics + the minimal
  `web-sys` features for canvas; measure a hello-cube binary size against NFR-7.
- **S3 (Phase 6):** confirm `wide` vs `pulp` vs `std::arch` for the rasterizer SIMD on
  stable (report 04 leaned `wide`/`pulp`); prototype the edge-function kernel.
- **S4 (Phase 6, stretch):** verify `wasm-bindgen-rayon` current status + a minimal
  COOP/COEP static-server recipe.

## 10. Open questions

- **Split or bundle?** Ship Phase 5 as **v1.1** and Phase 6 as **v2.0**, or both as one
  **v2.0**? (Default here: bundle as v2.0; Phase 5 is split-able if you want a faster win.)
- **Browser input scope:** mouse orbit only, or also touch + on-screen controls for mobile?
- **Demo hosting:** GitHub Pages from `web/`, or just a local-serve script in the repo?
- **Second theme confirm:** Performance (assumed) vs rendering-fidelity vs DSL/UX for the
  v2.0 pairing.
