# Third-Party Licenses

This project ([`tte`](README.md)) statically links the Rust crates below into the
`tte` binary and the WebAssembly demo. Each is redistributed under its own
permissive license, reproduced here per its attribution terms. Generated by
`cargo about generate about.hbs` (see `about.toml`); do not edit by hand.

{{#each licenses}}
## {{name}}

Used by:
{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{{text}}}
```

{{/each}}
