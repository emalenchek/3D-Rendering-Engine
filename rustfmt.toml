# Formatting config for `cargo fmt`. Conservative, stable-only options so that
# `cargo fmt --check` is a reliable CI gate without requiring nightly rustfmt.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
